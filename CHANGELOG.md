# 更新日志

本项目版本遵循语义化版本(主.次.补丁)。1.0 之前处于快速迭代期,次版本号随功能递增。

## [0.7.0] - 2026-06-16

### 新增

- 保存灵签:结束页可生成一张极简水墨签文卡(Canvas,1080×1440),移动端走系统分享面板存图/兜底长按保存到相册;埋点 `save_sign`

### 优化

- 抽签界面:签条由细圆杆(像火柴)改为扁平竹片,高低错落;抽出的竹签带朱砂签头与竹节纹、升起时微倾
- 文案:AI 解签入口「问神明·AI解签」→「AI 解签」、生成按钮「请神明解签」→「解此签」(解签是解签师的活,不归神明)

## [0.5.0] - 2026-06-16

### 新增

- AI 智能解签:结果页「问神明·AI 解签」→ 选 12 类问事门类(去诉讼)→ 引导式问答(首问 chips 必答 + 选填补充)→ 流式个性化解读
- `api/interpret`:Edge 流式函数,OpenAI 兼容(默认智谱 GLM),provider 由环境变量 `AI_BASE_URL/AI_API_KEY/AI_MODEL` 决定
- 系统提示边界铁律(禁医疗/投资/法律断言等)+ 页面免责声明
- `ai_interpretations` 表 + 服务端每日封顶(与求签共用 3 次,防刷接口)
- 埋点新增 `ai_interpret`

### 部署需先做

- 跑 `supabase/migrations/004_ai_interpret.sql`
- Vercel 配环境变量 `AI_API_KEY`(智谱 key);可选 `AI_BASE_URL`、`AI_MODEL`

## [0.4.0] - 2026-06-16

### 新增

- 每日求签上限:每北京自然日每访客最多 3 次完成的求签;超限欢迎页显示"今日已求三签",当天已求过会提示剩余次数
- 埋点新增 `limit_reached` 事件,量化"想多求"的需求(为后续解锁/付费提供数据)
- `APP_VERSION` 常量(带 typeof 兜底,避免未注入时白屏)

### 说明

- 本期为客户端 localStorage 软限制(符合"防多抽、求心诚");硬性服务端校验待接入 AI/付费时再加

## [0.3.0] - 2026-06-14

### 新增

- 按钮埋点:9 个按钮的曝光(show)/点击(click),session 公参,摇一摇/点击 trigger 区分
- 分析层 SQL:每日新增/活跃、留存、漏斗、摇一摇分布视图(供 Metabase 接入)
- 开发基建:`CLAUDE.md` 项目约定、Prettier、CHANGELOG、页面版本角标
- 数据库迁移重新纳入 git(顺序编号、增量向后兼容)

### 工作流

- 启用「功能分支 → Vercel Preview → PR → 合并 main」流程

## [0.2.0] - 2026-06

### 新增

- 接入 Supabase:访客(IP/地理位置)与求签记录落库,RLS 全锁 + security definer RPC
- `/api/geo`:Vercel edge 函数读地理头,替代国内不可达的第三方 IP 服务
- 结束页分享:复制个性化文案 + 链接,适配微信/小红书裂变

### 修复

- 确认茭笑筊改为「再掷一次」,不再误判为放回重抽
- 签诗竖排按句拆列对齐,修复错行错列
- Vercel 构建:为 vite.config 的 process.env 补 @types/node

## [0.1.0] - 2026-06

### 新增

- 关帝灵签求签解签 Web 应用:请示掷茭 → 抽签 → 确认 → 解签 → 叩谢 → 再求 完整仪式
- 摇一摇 + 点击双交互;100 签全文(爬虫生成);水墨禅意视觉
- 部署 Vercel
